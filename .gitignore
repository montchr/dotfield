git/dot-gitconfig.local

# Copy the Brewfile from one of the OS-specific example files.
/Brewfile
/Brewfile.lock.json

# Ignore repos contained within
emacs/emacs.d

# Karabiner Elements directory contains numerous backup and assets files we
# don't want. `karabiner.json` can't be symlinked directly because the
# software overwrites this file upon modifying settings
karabiner/*
!karabiner/install.sh
!karabiner/karabiner.json

# Emacs
/emacs/org/roam/org-roam.db

# Allow font archives.
!/iosevka/**/*.tar.gz

# ZSH
/zsh/.zinit/*
!/zsh/.zinit/plugins/_local*
