# Nix
cachix/cachix.dhall
result

# Auto-generated cache files from npm.
/configstore/update-notifier-*

# Managed by Nix
/bash/*
/bat/*
/doom/*
/emacs/*
/git/config-nix
/ripgrep/*
/shell/*
/skhd/*
/tealdeer/*
/yabai/yabairc
/zsh/*

# Composer
/composer/keys.*.pub
/composer/**/.htaccess

# Karabiner Elements directory contains numerous backup and assets files we
# don't want. `karabiner.json` can't be symlinked directly because the
# software overwrites this file upon modifying settings.
karabiner/*
!karabiner/karabiner.json

# Symlinked to currently-active theme (changes frequently).
/kitty/theme.conf

# Emacs
org-roam.db
custom.el
# flycheck_config.el

# Local files
*.local

# CI
/.ci/.terraform/
/.ci/*.tfvars
/.ci/*.tfstate*
