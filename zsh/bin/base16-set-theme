#!/usr/bin/env zsh
#
# Set the base16 theme.
#

emulate -L zsh
setopt extended_glob warn_create_global typeset_silent \
        no_short_loops rc_quotes no_auto_pushd

local THEME

THEME=$1

base16_${THEME}

. ${ZINIT[PLUGINS_DIR]}/nicodebo---base16-fzf/bash/base16-${THEME}.config
