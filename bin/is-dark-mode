#!/usr/bin/env zsh
#
# Check whether the system is in dark mode.
#

emulate -L zsh
setopt extended_glob warn_create_global typeset_silent \
        no_short_loops rc_quotes no_auto_pushd

print ${${(M)$((( $+commands[dark-mode] )) && dark-mode status):#on}:+1}
