{
  hardware.asahi.useExperimentalGPUDriver = true;
  # NOTE: "replace" mode requires impure eval!  but "overlay" requires
  # rebuilding the world.
  hardware.asahi.experimentalGPUInstallMode = "replace";
}
